1.对于one to one 的两张表
在A表插入数据的同时，是否必须也要向B表插入一条数据
2.在自己的service里是不是不要用别人的repository
3.MovieProject里，每张表的id为什么有关联？不是按照自己表的id从0开始，而是每张表公用一个id池。
4.如果请求接口时，报错了。在这个过程中，如果向好几个表中添加数据，在程序报错前，数据都能加上，报错后
，有的表的数据就加不上了。这就造成了数据不完整的问题。最好是要么都加上，要么一张表都不加。不要有的表
加了，有的表没加。
5.JpaRepository默认的findById(),到底用的哪个Id？如果一个实体类里有多个字段被@Id所注释，怎么办
6.同样的参数，访问多次添加电影接口，会添加几部一样的电影，但是movieNo是自增长的，所以会看作是不同
的影片
7.请求接口时，加影片类型id和演员id，很烦人，需要记忆。但如果不加的话，如果出现同名的演员怎么办。
所以Id才是唯一的标识，这个Id不同于主键自增的Id。
8.重新组装返回的数据，把演员之类的，都加进入，类型
9.Book findByName(String name);  如果有多个同名，是不是报错？
反过来添加不行，只能用book类添加数据，不能用bookNo类添加数据，虽然第二种方式也会在两张表中插入数据，但book表的bookNoId
字段没有值。意思是只能用主表的类来添加数据。详见addBookPro和addBookPro2。
10.一对多，多对一，关联不起来。没有一的关系。会有重复值。比如两个小说

4张表：


        电影编号表（电影编号,发行日期,发行国家）
                  one  to one
        电影表（id,电影名,电影类型id,电影编号,演员阵容） many to one  电影类型表（电影类型id,电影类型,电影类型特点）
                    many to many
        演员表（id,姓名,性别,年龄,参演电影）



需求：增：
        在电影表中插入一条数据，同时在电影编号表中插入一条数据
        （是否需要）查看电影类型表，如果没有当前类型，则新增一个  但是不一定有详细数据
        （是否需要）查看演员表，如果没有当前演员，则新增一个
   删：牵一发而动全身？
    电影表中删除一条数据，电影编号表同样删除，演员表中删不删？
    删除电影类型表中的一条数据，电影表中相关的怎么处理
   所以轻易不删除，只是隐藏
改：影响不不大

查：查看一个电影信息，是否把发行日期，发行国家，每个演员的详细信息，甚至是电影类型特点展示出来？
left join  ,  right join
